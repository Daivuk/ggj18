function updateInit(dt)
{
    PICKUP_HERO_COLLISION_RADIUS = 4 * TILE_HEIGHT;
    CENTRE_POSITION = new Vector2(17 * TILE_HEIGHT + HALF_TILE_HEIGHT, 8 * TILE_HEIGHT + HALF_TILE_HEIGHT);

    gameState = GameStateEnum.MAIN_MENU
}

function renderInit()
{
    
}